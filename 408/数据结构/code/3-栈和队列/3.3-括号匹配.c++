#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#define MaxSize 10
typedef struct
{
    // 静态数组存放栈元素
    char data[MaxSize];
    // 栈顶指针
    int top;
} SqStack;

bool bracketCheck(char str[], int length)
{
    SqStack S;
    // InitSqQueue(S);
}

int main()
{
    // bracketCheck()
}